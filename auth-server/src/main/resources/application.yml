spring:
  profiles:
    default: default
  application:
    name: auth-server

  config:
    import: "configserver:http://localhost:7777"

  datasource:
    url: jdbc:mysql://localhost:3307/security_db?serverTimezone=America/Lima&useSSL=false
    username: root
    password: S3cur1tyM1cr0S3rv1c3
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update

server:
  port: 3030
  servlet:
    context-path: /auth-server

eureka:
  instance:
    instance-id: "${spring.application.name}:${random.value}"
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/