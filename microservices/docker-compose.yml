version: '3.8'

services:

    db:
      image: postgres:16.1
      container_name: db-practicaspp
      restart: always

      environment:
        POSTGRES_DB: 'ppp'
        POSTGRES_USER: 'fourkodika'
        POSTGRES_PASSWORD: '*practicas123@*'
      ports:
        - '5433:5432'