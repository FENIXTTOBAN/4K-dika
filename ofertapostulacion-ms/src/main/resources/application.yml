spring:
  application:
    name: ofertapostulacion-ms

  datasource:
    url: jdbc:postgresql://localhost:5434/ofertas_db
    username: ofertas
    password: Of3rt4sM1cr0s3rv1c3
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 20000
      maximum-pool-size: 5

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE

server:
  port: 0
  servlet:
    context-path: /ofertapostulacion

eureka:
  instance:
    instance-id: "${spring.application.name}:${random.value}"
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/