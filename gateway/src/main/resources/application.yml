spring:
  application:
    name: gateway
  profiles:
    default: eureka-on  #eureka-off, eureka-on, oauth2
server:
  port: 4040


eureka:
  client:
    fetch-registry: true #luego true
    service-url:
      defaultZone: ${EUREKA_URL}